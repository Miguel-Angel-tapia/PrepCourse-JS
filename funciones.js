function sumatres(x) {
  console.log(x + 3);
}
sumatres(5)

// en este ejemplo, la estructura basica es:

// la palabra reservada function. indicando que estamos declarando una function

// la palabra sumatres: es el nombre que le estamos dando a nuestra function

// dentro de el parentesis, es el espacio para los parametros,o argumentos, le damos informacion a nuestra funcion

// dentro de las llaves, es el cuerpo de la funcion. aqui indicamos las intruciones que ejecutara la funcion







//el console.log solo lo usamos para ayudarnos en el proceso, ninguna aplicacion o pagina web debe tener un console.log en sus documentos, usamos la instruccion return para obtener el valor que queremos devolver

//Una instrucción return termina la ejecución de una función y devuelve el control a la función de llamada. La ejecución se reanuda en la función de llamada, en el punto que sigue inmediatamente a la llamada.